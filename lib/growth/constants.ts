/**
 * Fixed sprint goals — plain constants, not stored/configurable values
 * (Mode 1: single founder, single stated goal, no settings UI needed).
 * Kept in one place so Mission Control's Mission Progress (PE-3) and Daily
 * Success Counter (PE-7) never quote two different numbers for the same
 * goal.
 */
export const PILOT_ACQUISITION_GOAL = 10 // August's "10 pilot schools" objective
export const WEEKLY_CONTACT_GOAL = 15 // schools contacted per week, a sprint-set execution target

import type { CountyConfig } from './types'
import { DEFAULT_SEARCH_TERMS } from './defaults'

export const kirinyagaConfig: CountyConfig = {
  county: 'Kirinyaga',
  slug: 'kirinyaga',
  towns: ['Kerugoya', 'Kutus', 'Kagio', 'Sagana', 'Baricho', 'Wanguru', 'Mwea', 'Kianyaga', 'Kagumo'],
  searchTerms: DEFAULT_SEARCH_TERMS,
}

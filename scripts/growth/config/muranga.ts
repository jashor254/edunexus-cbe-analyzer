import type { CountyConfig } from './types'
import { DEFAULT_SEARCH_TERMS } from './defaults'

export const murangaConfig: CountyConfig = {
  county: "Murang'a",
  slug: 'muranga',
  towns: ["Murang'a", 'Kenol', 'Kandara', 'Kangema', 'Maragua', 'Kiria-ini', 'Kigumo'],
  searchTerms: DEFAULT_SEARCH_TERMS,
}
